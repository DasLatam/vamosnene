---
import Base from "../layouts/Base.astro";
import NewsFeed from "../components/NewsFeed.astro";
const api = import.meta.env.PUBLIC_API_BASE;

const r = await fetch(`${api}/api/news?only_colapinto=1&limit=12&offset=0`);
const initial = await r.json();
---
<Base title="Noticias (Colapinto) — Vamos Nene...!!!" description="Noticias filtradas de Colapinto (título + texto), con imágenes cuando la fuente las publica.">
  <section class="hero">
    <div class="heroInner">
      <div>
        <div class="kicker">📰 Noticias · Colapinto</div>
        <h1 class="h1">Noticias (Colapinto)</h1>
        <p class="p">
          Mostramos titulares con crédito + link a la fuente. Sumamos <b>Notas de la Redacción</b> (paráfrasis/contexto) para que tenga sentido en Argentina.
        </p>
        <div class="actions">
          <a class="btn primary" href="/vivo">🟣 Volver a Vivo</a>
          <a class="btn alt" href="/suscribirme">🔔 Suscribirme</a>
        </div>
      </div>
      <div>
        <div class="card" style="height:100%;">
          <div class="cardHeader">
            <h2 class="cardTitle">Fuentes</h2>
            <p class="cardSub">RSS (con crédito) + filtro Colapinto.</p>
          </div>
          <div class="cardBody">
            <div class="small">
              Si una fuente viene mal codificada, arreglamos el charset (para evitar “present�”).
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <div style="margin-top:14px;">
    <NewsFeed initial={initial} title="Feed" />
  </div>
</Base>
